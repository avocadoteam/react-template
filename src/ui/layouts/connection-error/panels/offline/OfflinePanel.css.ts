import { style } from '@vanilla-extract/css';

export const offline = style({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  overflow: 'hidden',
});
export const offlineDescription = style({
  marginTop: '16px',
});
